# 리즈워드 홈페이지 콘텐츠 업데이트 메뉴얼

뉴스 · 유튜브 · 교육 사례를 직접 업데이트하는 방법입니다.
아래 템플릿을 **그대로 복사해서 값만 바꿔 넣으면** 됩니다.

---

## 0. 공통: 수정하고 반영하는 방법

수정 방법은 두 가지 중 편한 것을 쓰면 됩니다.

**방법 A — GitHub 웹에서 직접 수정**
1. https://github.com/liszzmword/lisword_homepage 접속
2. 수정할 파일 클릭 → 오른쪽 위 연필(✏️) 아이콘 클릭
3. 내용 수정 → 오른쪽 위 초록색 **Commit changes** 버튼 → 다시 **Commit changes**
4. 30초~1분 뒤 www.lisword.co.kr 에 자동 반영 (Vercel 자동 배포)
5. 바로 안 보이면 브라우저 강력 새로고침: **Cmd + Shift + R** (Mac) / **Ctrl + Shift + R** (Windows)

**방법 B — Claude에게 요청**
Claude Code를 열고 이렇게 말하면 됩니다:
> "뉴스 추가해줘. 제목: ○○○, 매체: ○○일보, 날짜: 2026-07-01, 링크: https://..."

⚠️ **직접 수정할 때 딱 두 가지만 조심하세요**
- 따옴표(`"`)와 쉼표(`,`)를 템플릿 그대로 유지할 것 — 하나라도 빠지면 페이지가 안 뜹니다
- 깨졌을 때: GitHub에서 해당 파일 → **History** → 직전 버전 → **Revert** 하거나, Claude에게 "홈페이지 깨졌어, 복구해줘"라고 하면 됩니다

---

## 1. 뉴스 (언론 보도) 추가

**파일: `news-data.js`**

`window.NEWS_ARTICLES = [` 바로 아랫줄에 아래 블록을 붙여넣고 값을 바꿉니다.
**새 기사일수록 위에** 있어야 홈 화면에 먼저 나옵니다 (홈에는 위에서 3개만 노출).

```js
  {
    id: "domino-rag-2026",
    title: "기사 제목을 그대로 입력",
    source: "매체 이름 (예: 대학저널)",
    date: "2026-07-01",
    url: "https://기사링크",
    excerpt: "기사 요약 한두 문장. 없으면 이 줄을 통째로 지워도 됩니다."
  },
```

| 항목 | 설명 |
|---|---|
| `id` | 영문 소문자와 하이픈(-)으로 아무거나. 다른 기사와 겹치지만 않으면 됨 |
| `date` | 반드시 `YYYY-MM-DD` 형식 (뉴스 전체보기 페이지가 이 날짜로 최신순 정렬) |
| `excerpt` | 선택사항. 홈 화면 카드에 요약으로 표시됨 |

---

## 2. 유튜브 영상 교체

**파일: `sections/youtube.jsx`**

파일 위쪽의 `YT_VIDEOS` 목록에서 교체할 줄을 통째로 바꿉니다. **항상 3개를 유지**하세요 (오래된 영상을 지우고 새 영상으로 교체).

```js
const YT_VIDEOS = [
  { videoId: "GxRfz9hdiLA", t: 437, title: "AI 교육, 이렇게 시작하세요" },
  { videoId: "7FLsVEdXeTc", t: 483, title: "실무에 바로 쓰는 AI 활용법" },
  { videoId: "X_tKxvPtuBU", t: 4,   title: "리즈워드가 풀어가는 AI 교육" },
];
```

**videoId 찾는 법** — 유튜브 영상 주소에서 뒷부분만 복사:
- `https://www.youtube.com/watch?v=GxRfz9hdiLA` → videoId는 **GxRfz9hdiLA**
- `https://youtu.be/GxRfz9hdiLA` → 마찬가지로 **GxRfz9hdiLA**

**t** = 썸네일 대신 재생 시작할 시간(초). 영상 처음부터면 `0`.
(예: 7분 17초부터 시작하고 싶으면 7×60+17 = `437`)

---

## 3. 교육 사례 (포트폴리오) 추가

**파일: `sections/portfolio.jsx`** + 현장 사진 1장

### ① 사진 먼저 올리기
1. GitHub 저장소 첫 화면에서 **Add file → Upload files**
2. 사진을 끌어다 놓고 **Commit changes**
3. ⚠️ 파일명은 **공백 없이** 지어주세요 (예: `LG이노텍_AI리터러시.jpg` ✅ / `LG이노텍 AI 리터러시.jpg` ❌)
   - 공백이 있으면 코드에 `%20`으로 바꿔 적어야 해서 번거롭습니다

### ② 카드 추가하기
`sections/portfolio.jsx`의 `const items = [` 안에서, 마지막 카드(`},`) 뒤에 붙여넣습니다:

```js
    {
      id: "lginnotek",
      num: "05",
      title: "임직원 AI 리터러시 교육",
      client: "LG이노텍",
      date: "2026",
      image: "LG이노텍_AI리터러시.jpg",
    },
```

| 항목 | 설명 |
|---|---|
| `id` | 영문 소문자로 아무거나 (겹치지만 않게) |
| `num` | 카드 번호. 기존 마지막 번호 +1 (01→02→03…) |
| `title` | 교육 이름 (예: "AI 부트캠프 — 창업·문제해결 트랙") |
| `client` | 기관·회사명 |
| `date` | 연도만 (예: "2026") |
| `image` | ①에서 올린 사진 파일명과 **정확히 동일하게** |

---

## 4. 그 외 자주 바꿀 만한 것들

| 바꾸고 싶은 것 | 파일 |
|---|---|
| 수강 후기 | `sections/reviews.jsx` |
| 함께한 기업 로고 | `sections/companies.jsx` + `logos/` 폴더 |
| 스킬트레이닝 4트랙 설명 | `sections/skills.jsx` |
| AX 전환 서비스 내용 | `sections/ax.jsx` |
| 문의 폼 (수신 메일 등) | `sections/contact.jsx` |
| 히어로 문구 | `app.jsx` 상단 `TAGLINES` |
| AI 검색용 회사 소개 | `llms.txt` |

형식이 헷갈리면 무리해서 직접 고치지 말고 Claude에게 맡기는 게 안전합니다.

---

## 5. 문의 폼 참고사항

- 문의 메일은 **Web3Forms**(무료, 월 250건)를 통해 발송됩니다
- 수신 메일 주소를 바꾸고 싶으면: web3forms.com에서 새 이메일로 액세스 키 발급 → `sections/contact.jsx` 5번째 줄의 `WEB3FORMS_ACCESS_KEY` 값 교체
